# WebCo Solutions - Лендинг веб-студии

Современный и красивый лендинг для веб-студии с формой обратной связи для сбора лидов.

## 🚀 Особенности

- **Современный дизайн** - Градиенты, анимации и плавные переходы
- **Адаптивная верстка** - Отлично выглядит на всех устройствах
- **Форма обратной связи** - Сбор лидов с валидацией
- **Информация о команде** - Секция с опытом и навыками
- **Анимации при скролле** - Элементы появляются плавно
- **Оптимизированная производительность** - Быстрая загрузка

## 📁 Структура проекта

```
my-studio-landing/
├── index.html      # Основная HTML структура
├── styles.css      # Стили и дизайн
├── script.js       # JavaScript функциональность
└── README.md       # Документация
```

## 🎨 Секции лендинга

1. **Hero** - Главный экран с призывом к действию
2. **О нас** - Преимущества и подход к работе
3. **Услуги** - Четыре основных направления
4. **Команда** - Информация о команде и опыте
5. **Форма обратной связи** - Сбор контактов и заявок
6. **Футер** - Дополнительная информация

## 🛠️ Использование

### Локальный запуск

1. Откройте `index.html` в браузере
2. Или используйте локальный сервер:

```bash
# Python 3
python -m http.server 8000

# Node.js (если установлен http-server)
npx http-server
```

Затем откройте `http://localhost:8000` в браузере.

## 📝 Настройка формы

В файле `script.js` функция `simulateFormSubmission` имитирует отправку формы. Для реальной работы:

1. Замените функцию на отправку данных на ваш backend
2. Или используйте сервисы типа:
   - Formspree
   - EmailJS
   - Google Forms API
   - Ваш собственный API

Пример интеграции с EmailJS:

```javascript
// Установите EmailJS: npm install @emailjs/browser
import emailjs from '@emailjs/browser';

async function submitForm(formData) {
    const templateParams = {
        name: formData.get('name'),
        email: formData.get('email'),
        phone: formData.get('phone'),
        service: formData.get('service'),
        message: formData.get('message')
    };
    
    await emailjs.send(
        'YOUR_SERVICE_ID',
        'YOUR_TEMPLATE_ID',
        templateParams,
        'YOUR_PUBLIC_KEY'
    );
}
```

## 🎨 Кастомизация

### Цвета

Измените CSS переменные в `styles.css`:

```css
:root {
    --primary-color: #6366f1;
    --secondary-color: #8b5cf6;
    --accent-color: #ec4899;
    /* ... */
}
```

### Контент

Отредактируйте тексты в `index.html`:
- Название студии
- Описания услуг
- Информацию о команде
- Контактные данные

### Команда

В секции "Команда" замените:
- Имена и роли
- Опыт и биографии
- Навыки (skill-tag)

## 📱 Адаптивность

Лендинг полностью адаптивен и оптимизирован для:
- Десктоп (1200px+)
- Планшет (768px - 1199px)
- Мобильные устройства (< 768px)

## 🔧 Технологии

- HTML5
- CSS3 (Grid, Flexbox, Animations)
- Vanilla JavaScript (ES6+)
- Google Fonts (Inter)

## 📄 Лицензия

Этот проект создан для использования в коммерческих целях.

## 💡 Рекомендации

1. **SEO**: Добавьте мета-теги в `<head>`
2. **Аналитика**: Подключите Google Analytics или Яндекс.Метрику
3. **Форма**: Настройте реальную отправку данных
4. **Изображения**: Добавьте реальные фото команды
5. **Фавикон**: Добавьте иконку сайта

## 📞 Поддержка

Если у вас есть вопросы или нужна помощь с настройкой, свяжитесь с нами!

---

Создано с ❤️ для WebCo Solutions

